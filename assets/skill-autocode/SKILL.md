---
name: autocode
description: autoworker 的兼容别名。建议迁移到 autoworker。
---

# autocode (compat alias)

`autocode` 现在只是 `autoworker` 的兼容别名。

- 新主名：`autoworker`
- 当前仍支持旧触发词：`autocode`
- 内部实现会转发到 `autoworker`
